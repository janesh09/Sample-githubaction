name: First Workflow
on: workflow_dispach
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      -name: Print greetings
       run: echo "Hello world"
      -name: Print Goodbye
       run: echo "Done"
      
